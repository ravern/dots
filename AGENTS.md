# Agent Instructions

- After installing new apps or tools, update either `Brewfile` or `bootstrap.sh` to keep the setup up to date.
- Prefer the file that best matches the install method (Homebrew apps in `Brewfile`, manual installs or custom steps in `bootstrap.sh`).
